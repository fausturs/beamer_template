\frametitle{\secname }
代码
    % 在lstlisting框内所有字符都会被输出
    % 所以代码前面要是有空格也会被输出，故而代码都没有缩进
    \begin{lstlisting}
#include<iostream>
int main()
{
    //c++
    std::cout<<"hello world!"<<std::endl;
    return 0;
}
$\pi^\pi$
$\$$
    \end{lstlisting}
    要用空格代替tab，frame要加上fragile选项。
    高亮也有点怪
